{
  "name": "mapViewer",
  "version": "1.0.0",
  "description": "Affiche la map voulu en mode tactique || réel",
  "main": "index.js",
  "author": "Drigtime",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "build-osx": "electron-packager ./ --electron-version 2.0.4 --platform darwin --out ./dist --overwrite",
    "build-linux": "electron-packager ./ --electron-version 2.0.4 --platform linux --out ./dist --overwrite",
    "build-win": "electron-packager ./ --electron-version 2.0.4 --platform win32 --out ./dist --overwrite"
  },
  "dependencies": {
    "electron": "^2.0.4",
    "jquery": "^3.3.1",
    "materialize-css": "^1.0.0-rc.2",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "electron-packager": "^12.1.0"
  }
}
